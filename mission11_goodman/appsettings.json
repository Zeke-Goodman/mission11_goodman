{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Default log level for the application
      "Microsoft.AspNetCore": "Warning" // Log level for Microsoft.AspNetCore namespace
    }
  },
  "AllowedHosts": "*", // Allows all hosts to access the application
  "ConnectionStrings": {
    "BookConnection": "Data Source=Bookstore.sqlite" // Connection string for the database named "BookConnection"
  }
}
